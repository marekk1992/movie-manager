spring:
  datasource:
    url: jdbc:tc:postgresql:15.1:///test_database
    username: user
    password: password
  test:
    database:
      replace: none
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
