spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: movieuser
    password: postgres123
    hikari:
      schema: movie_directory
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect